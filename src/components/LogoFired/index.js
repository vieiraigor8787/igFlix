import styled from 'styled-components';
import logo from '../../assets/imagem/Logo.png'

const LogoFired = styled.img.attrs({ src: logo, alt: 'Logo da igFlix' })`
    height: 60px;
`;

export default LogoFired;